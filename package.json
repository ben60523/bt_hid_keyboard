{
  "name": "bt_hid_keyboard",
  "version": "0.0.0",
  "description": "wifi scan with wlanapi",
  "main": "index.js",
  "private": true,
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^7.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.2.0"
  },
  "scripts": {
    "build": "npx node-gyp rebuild",
    "config": "npx node-gyp configure -- -f compile_commands_json && cp build/Release/compile_commands.json .",
    "test": "node test.js"
  },
  "gypfile": true
}
